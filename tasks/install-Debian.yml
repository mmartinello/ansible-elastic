---
- name: Install dependencies
  ansible.builtin.apt:
    name: apt-transport-https
    state: present

- name: Download Elastic GPG key
  ansible.builtin.get_url:
    url: https://artifacts.elastic.co/GPG-KEY-elasticsearch
    dest: /tmp/GPG-KEY-elastic
    mode: '0644'

- name: Install Elastic GPG key
  ansible.builtin.command:
    cmd: gpg --dearmor -o /usr/share/keyrings/elastic-keyring.gpg /tmp/GPG-KEY-elastic
  args:
    creates: /usr/share/keyrings/elastic-keyring.gpg

- name: Remove Elastic GPG key temporary file
  ansible.builtin.file:
    path: /tmp/GPG-KEY-elastic
    state: absent

- name: Add Elastic APT Repository
  ansible.builtin.apt_repository:
    repo: 'deb [signed-by=/usr/share/keyrings/elastic-keyring.gpg] https://artifacts.elastic.co/packages/{{ metricbeat_major_version }}.x/apt stable main'
    filename: elastic
    state: present
    update_cache: true
